<!-- 占位符 -->
<view style="height: 2.5rem;"></view>

<!-- 房源列表 -->
<view class="title">市场租房</view>

<view class="cu-bar bg-white solid-bottom">
  <view class='action'>
    <text class='cuIcon-titles text-orange '></text> 最新房源信息
  </view>
  <button class="cu-btn text-bold icon bg-black margin-left" bindtap="addNewRent">
    <text class="cuIcon-add text-white"></text>
  </button>
</view>

<scroll-view 
  wx:for="{{ articles }}" 
  wx:key="_id"  
  bindtap="goToDetail" 
  data-id="{{item._id}}" 
  class="cu-card case {{ isCard? 'no-card' : '' }}"
>
  <view class="cu-item shadow">
    <view class="image">
      <image src="{{ item.images[0] }}" mode="widthFix"></image>
      <view class="cu-tag bg-blue">史诗</view>
      <view class="cu-bar bg-shadeBottom">
        <text class="text-cut">{{ item.title }}</text>
      </view>
    </view>
    <view class="cu-list menu-avatar">
      <view class="cu-item">
        <view class="cu-avatar round lg" style="background-image:url({{item.user_avatar}});"></view>
        <view class="content flex-sub">
          <view class="text-grey">{{item.user_nickname}}</view>
          <view class="text-gray text-sm flex justify-between">
            {{item.timedistance}}
            <view class="text-gray text-sm">
              <text class="cuIcon-attentionfill margin-lr-xs"></text> {{item.view}}
              <!-- <text class="cuIcon-appreciatefill margin-lr-xs"></text> {{item.like_count}} -->
              <text class="cuIcon-messagefill margin-lr-xs"></text> {{item.comment_count}}
            </view>
          </view>
        </view>
      </view>
    </view>
  </view>
</scroll-view>
<!-- tabBar -->
<tabBar bindswitchtab="redirect" tab="rentList"></tabBar>